#include <iostream>
using namespace std;

int opposite(int n);

int main() {

  int greatest = 0;

	for (int i = 100; i <= 999; i++) {

		for (int j = 100; j <= 999; j++) {

			if (i*j==opposite(i*j) and i*j > greatest)

				greatest = i*j;
		}

	}

	cout << greatest;

	return 0;
}

int opposite(int n) {

	int m = 0;

	while (n > 0) {

		m = m * 10 + n % 10;
		n = n / 10;
	}

	return m;
}
# Find the largest palindrome
result = find_largest_palindrome()

# Save the result in the file
with open("https://github.com/MUTESA20/alx-low_level_programming/blob/master/0x17-doubly_linked_lists/102-result", "w") as file:
    file.write(str(result))
